OpenNode CLI utilities
----------------------

Installation
============

1) yum install python-devel  gcc gcc-c++ autoconf automake

2) pip install -r requirements.txt

3) yum install newt-python

Running
=======
1. Make sure opennode module is on the PYTHONPATH, e.g. PYTHONPATH=`pwd`:$PYTHONPATH
2. ''scripts/opennode''
